#include<stdio.h>
#include<stdlib.h>

struct node{
    int data;
    struct node *link;
};

int main() {
    struct node head = (struct node*)malloc(sizeof(struct node));
    struct node node1 = ;
    struct node node2 = NULL;


    return 0;
}