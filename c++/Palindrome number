//wap to check whether the number is palindrome or not
#include<iostream>
using namespace std;
int main()
{
int num,rev=0;
int remainder'
cout<<"Enter the palindrome number "; //Four output
cin>>num; 
int temp=n;
while(num>0)
{
remainder=rev*10
rev=remainder+num%10;
num/=10;
}
if (temp==rev)
{
cout<<"Number is palindrome ";
}
else
{
cout<<"Number is not palindrome";
}
return 0;
}
